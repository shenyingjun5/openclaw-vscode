# OpenClaw VSCode Extension v0.2.10

## 🔗 链接渲染优化 / Link Rendering Improvements

- Markdown 自动链接 `<https://...>` 语法支持 / Markdown auto-links support
- 纯文本 URL 自动检测为可点击链接 / Plain-text URL auto-detection
- 代码块和行内代码中的 URL 不会被错误转换（占位符机制） / Code block protection (placeholder mechanism)
- 外部链接通过系统默认浏览器打开 / External links open in system browser

## 🔌 重连状态反馈 / Reconnect Status Feedback

- 重连成功后状态灯立即变绿，清除错误信息 / Green light immediately after successful reconnect
- 重连失败时更新最新错误信息到状态弹窗 / Error message updates on reconnect failure

## 📦 安装 / Install

```bash
# 从 GitHub Release 下载 / Download from GitHub Release
# 或从 Open VSX 搜索 "OpenClaw" / Or search "OpenClaw" on Open VSX
```
